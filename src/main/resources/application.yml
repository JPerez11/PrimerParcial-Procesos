
#API config

server:
  port: 8081

# Database config

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/test
    username: root
    password: admin

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

